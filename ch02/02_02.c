    #include <stdio.h>
    int main(void)
    {
    printf("-------------------print type size----------------------------\n");
    printf("sizeof unsigned int type size is (%d)\n",sizeof(unsigned int));
    printf("sizeof int type size is (%d)\n",sizeof(int));
    printf("sizeof char type size is (%d)\n",sizeof(char));  
    printf("sizeof short type size is (%d)\n",sizeof(short));
    printf("sizeof unsigned short type size is (%d)\n",sizeof(unsigned short));
    printf("sizeof doblue type size is (%d)\n",sizeof(double));
    printf("sizeof folat type size is (%d)\n", sizeof(float));
    printf("sizeof long type size is (%d)\n",sizeof(long));
    printf("sizeof long long type size is (%d)\n",sizeof(long long));
    printf("sizeof unsigned long type size is (%d)\n",sizeof(unsigned long));
    printf("sizeof long* type size is (%d)\n",sizeof(long*));
    printf("sizeof int* type size is (%d)\n",sizeof(int*));
    printf("sizeof char* type size is (%d)\n",sizeof(char*));
    printf("sizeof wchar_t type size is (%d)\n",sizeof(wchar_t));
        return 0;
    }
    
  